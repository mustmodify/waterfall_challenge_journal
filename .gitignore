.codegpt
waterfall-journal
wc-journey
.mcp.json
test/features.json

# Archived pages we re-read locally rather than re-fetching
data/spider-cache/
data/alltrails/raw/

# Secrets live in the shell or the host, never in the tree
.env
.env.*
__pycache__/
